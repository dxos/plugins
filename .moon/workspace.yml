# https://moonrepo.dev/docs/config/workspace
$schema: https://moonrepo.dev/schemas/workspace.json

# Each plugin under packages/* is a moon project (id = directory name, e.g. `tictactoe`).
projects:
  - 'packages/*'

vcs:
  client: git
  defaultBranch: main
